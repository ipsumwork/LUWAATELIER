# Sanity CMS Configuration
# Create a project at https://sanity.io/manage

NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# API token for preview mode (create at sanity.io/manage > API > Tokens)
SANITY_API_READ_TOKEN=your-read-token

# Secret for preview mode URL validation
SANITY_PREVIEW_SECRET=your-preview-secret

# Secret for webhook signature validation
SANITY_REVALIDATE_SECRET=your-webhook-secret

# Visual Editor Configuration
# Password for entering edit mode (navigate to /?edit=true)
EDITOR_SECRET=your-editor-secret

# API token with write permissions (create at sanity.io/manage > API > Tokens)
# Required for publishing changes from the visual editor
SANITY_API_WRITE_TOKEN=your-write-token
